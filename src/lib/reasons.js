// Common reasons for a dental visit, shared by the public booking flow and the
// doctor's own scheduling form. Each entry is a translation key; the visible
// label (and the value stored on the appointment) is the translated string,
// so the doctor immediately knows what to prepare for.
export const VISIT_REASON_KEYS = [
  "reason.cleaning",
  "reason.checkup",
  "reason.filling",
  "reason.whitening",
  "reason.endo",
  "reason.ortho",
  "reason.extraction",
  "reason.pain",
  "reason.other",
];
